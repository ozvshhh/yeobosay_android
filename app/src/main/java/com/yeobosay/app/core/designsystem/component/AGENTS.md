# Component Rules / 공통 컴포넌트 규칙

이 디렉토리는 재사용 가능한 UI 컴포넌트를 담당합니다.

## 규칙

- 이곳의 컴포넌트는 둘 이상의 feature에서 사용할 수 있어야 합니다.
- 특정 feature 전용 컴포넌트는 `feature/<name>/components/`에 둡니다.
- 컴포넌트에서 API 호출을 수행하지 않습니다.
