# Legacy UI Package Notice / 기존 UI 패키지 안내

이 패키지는 기존 Compose UI 코드를 담고 있습니다.

## 마이그레이션 방향

- 새 화면 단위 작업은 `feature/` 아래에 추가합니다.
- 공통 theme와 재사용 컴포넌트는 `core/designsystem/`으로 이동하는 방향을 따릅니다.
- 이 패키지 아래에 새 대형 화면을 직접 추가하지 않습니다.
